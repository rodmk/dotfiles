{{- /* chezmoi:modify-template */ -}}
{{- $config := .chezmoi.stdin | fromToml | default (dict) -}}
{{- $config
    | setValueAtPath "project_doc_fallback_filenames" (list "CLAUDE.md")
    | setValueAtPath "tui.terminal_title" (list "activity" "project" "thread-title")
    | toToml
-}}
