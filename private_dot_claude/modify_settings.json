#!/bin/bash
# chezmoi modify script: merge statusLine config into existing settings.json
content=$(cat)
[ -z "$content" ] && content='{}'
echo "$content" | jq '.statusLine = {"type":"command","command":"sh ~/.claude/statusline-command.sh"}'
