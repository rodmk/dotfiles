## GitHub

- Always open PRs in draft mode (`--draft`).
- Always update PR title and summary to reflect all changes on the branch.
- Never @ mention or name individuals. Only @ mention in PR comments with explicit approval.
- Code references must use permalink SHAs and exact line numbers. Use `gh api`/`curl`, not WebFetch (it silently mutates content).
